applications:
- name: movie-recommender-flask
  host: movie-recommender-demo
  memory: 128M
  instances: 1
  path: .
  command: python manage.py runserver
  # to run in debug mode:
  # command: python manage.py runserver -d
  services:
  - my_cloudant
  - my_redis
  - my_messagehub
